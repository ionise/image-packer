# Windows Server 2019 (stub)

This version is not yet implemented. To scaffold it, copy `builds/windows/2022`,
rename the files to `windows-2019*`, set the build block `name` to `windows-2019`,
update the ISO URL/checksum, and set the image name in `http/Autounattend.xml` to
`Windows Server 2019 SERVERSTANDARD`.

See [docs/structure.md](../../../docs/structure.md).
