# Static files for Windows images (optional)

Drop any static files you want copied into Windows images here (e.g. agents,
certificates, baseline configs). Reference them from a `file` provisioner in the
relevant `*.pkr.hcl` build block.

This folder is a placeholder; remove it if unused.
